openapi: 3.0.0
info:
  title: --
  description: --
  version: --

paths:
  /default/hello.sql:
    get:
      operationId: get__default_hello
      description: Read data
      responses:
        "200":
          description: Sucess
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    message:
                      type: string
