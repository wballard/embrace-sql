/* eslint-disable @typescript-eslint/camelcase */
import * as types from "../context";

export const before: types.default_helloBeforeHandler = async (context) => {
  // handle me!
  return context;
};
