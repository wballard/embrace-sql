---
to: "{{module.fullPath}}.afterError.ts"
---

/* eslint-disable @typescript-eslint/camelcase */
import * as types from "{{relativeToRoot}}/context";

export const afterError : types.{{module.contextName}}Handler = async (context) => {
  // handle me!
  return context;
}